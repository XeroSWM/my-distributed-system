# --- TERRAFORM (Lo más importante para que no pese) ---
# Ignorar la carpeta de plugins (esto es lo que pesa mucho)
**/.terraform/

# Ignorar estados locales (contienen datos sensibles)
*.tfstate
*.tfstate.*

# Ignorar logs de fallos
crash.log
crash.*.log

# Ignorar archivos de variables con SECRETOS (¡Muy importante!)
*.tfvars
*.tfvars.json
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Ignorar configuraciones de CLI locales
.terraformrc
terraform.rc


# --- NODE.JS / JAVASCRIPT (Frontend y Backends) ---
# La carpeta más pesada del universo
**/node_modules/

# Logs de npm
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Builds de producción (dist, build, etc)
**/dist/
**/build/
**/coverage/


# --- VARIABLES DE ENTORNO (Secretos) ---
.env
.env.local
.env.development.local
.env.test.local
.env.production.local


# --- SISTEMA OPERATIVO E IDE ---
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp